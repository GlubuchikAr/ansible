---
- name: Vector| Install package
  ansible.builtin.include_tasks:
    file: install_{{ ansible_facts.pkg_mgr }}.yml
- name: Vector | Configure vector
  ansible.builtin.include_tasks:
    file: configure_vector.yml
- name: Vector | Configure service
  ansible.builtin.include_tasks:
    file: configure_service.yml
# - name: Start Vector (если systemd есть)
#   ansible.builtin.service:
#     name: vector
#     state: started
#     enabled: true
#   when: ansible_facts.service_mgr == "systemd"
# - name: Start Vector manually (если нет systemd)
#   ansible.builtin.command: "/usr/bin/vector --config {{ vector_config_dir }}/vector.yml"
#   when: ansible_facts.service_mgr != "systemd"